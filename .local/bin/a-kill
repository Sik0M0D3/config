#!/bin/sh

# Check running processes and kill 'em all.
(pidof picom) && kill $(pidof picom)
(pidof dwmblocks) && kill $(pidof dwmblocks)
(pidof dunst) && kill $(pidof dunst)
(pidof sxhkd) && kill $(pidof sxhkd)
