#!/bin/sh
# This script is to run One-Shot programs with no persistance.

# Clear old screen temperature and set to 5000 the actual.
gammastep -x -m randr && gammastep -O 5000 -m randr

# Set wallpaper with xwallpaper tool.
x-wset --zoom ~/Pix/"Some Picture Here"
